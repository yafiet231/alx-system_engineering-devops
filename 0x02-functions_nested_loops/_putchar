#include <unistd.h>


/**
 * 0-putchar - the character c to stdout
 * @c: the character to print
 * return: on success 1.
 * on error, -1 is returned, and error is set aproprate
 */

int 0-putchar(char c)
{

        return (write(1, &c, 1));
}

